# Cardiac Solutions LLC - AED Monitoring Dashboard PRD

## Original Problem Statement
Build a Tony Stark, dark themed web page for Cardiac Solutions LLC. They sell, service and monitor AEDs in the USA using remote cameras.

## What's Been Implemented
- [x] Login page with animated SVG heart, EKG line, sound effects, build version display
- [x] Command Center Hub with "Powering Up" splash screen and dynamic module cards
- [x] MongoDB user storage with PBKDF2 hashed passwords (7 seeded users)
- [x] Role-based access control (admin/user roles)
- [x] Dashboard Assignment System — per-user dashboard type ("standard" or "support")
- [x] User Access admin page (full CRUD + Dashboard Type dropdown)
- [x] Standard Dashboard with dynamic status LEDs, DI scrolling, OpenAI TTS preload
- [x] **Support Dashboard** with:
  - Fleet stats (subscribers, issues by type)
  - Sortable table: EXP B/P, EXPIRING, NOT RDY, REPOS, TOTAL columns
  - Real-time data from Readisys Voice API per-subscriber status
  - Notification modal with email template from user's .docx
  - Camera images from Readisys `/voice/aed/{id}/last-image` API
  - Device detail columns: Batt/Pads Exp, Battery %, Signal, Capture Timestamp
  - **Device Detail Drawer** — click any device row to see full-size image, all device info, diagnostics, and editable notes (saved to MongoDB)
  - **Notification History** — view all sent emails with subscriber filter, manual status override, notes, sorting
  - **Editable Details per AED** — support reps can customize detail text per device before sending; reflected in Mailgun HTML
  - Clickable issue counts open DeviceListModal; interactive stat card filters with progress bars
  - Status Correction Feedback modal; Auto CC tprince@cardiac-solutions.net
  - Email subject: "AED Report and Action Items"
  - Section label: "AED Batteries and Pads Expired/Expiring"
  - 51 subscriber contacts seeded from Excel + Readisys name variants
  - Contacts modal with SET/EMPTY badges, search, alphabetical sort
- [x] Backend Management page with live Readisys data
- [x] Service Console with parallel API loading, ticket CRUD, dispatch, tech response

## Key API Endpoints
- `GET /api/support/dashboard-data` — aggregated subscriber issues + readiness stats (actual & adjusted %)
- `GET /api/support/subscriber/{name}/devices` — all devices for subscriber
- `GET /api/support/aed-image/{sentinel_id}` — camera image metadata
- `POST /api/support/send-notification` — send email via Mailgun + track notified AEDs in DB
- `GET /api/support/notification-history` — sent email history with optional subscriber filter
- `GET /api/support/notified-aeds` — list all tracked notified AEDs
- `GET /api/support/notified-aeds/summary` — summary stats (total, unresolved, resolved, breakdowns)
- `POST /api/support/notified-aeds/refresh` — manually trigger status refresh from Readisys
- `GET/PUT /api/support/device-notes/{sentinel_id}` — device notes CRUD

## Tech Stack
- Frontend: React 19 + Framer Motion + Tailwind CSS + Shadcn UI
- Backend: FastAPI + Motor (async MongoDB) + PBKDF2 + PyJWT + httpx
- Database: MongoDB
- Integrations: Mailgun (email), OpenAI TTS (Emergent LLM Key), Readisys API (AED data + images)

## Credentials
- **Admin**: futureroads / @@U1s9m6c7@@
- **Support User**: Lew / Lew123 (dashboard_type: support)

## Prioritized Backlog
### P1 (High)
- Build Daily Report module page
- Build Notifications module page
- View past images for a device (needs Readisys API check)

### P2 (Medium)
- Build Survival Path module page

### P3 (Nice to Have)
- Refactor server.py (~2600 lines) into modular route files
- Refactor large frontend components

## Changelog
- 2026-05-04: **Sales — Fuel Estimate (miles + gallons + $)** — New "FUEL EST" button (emerald, top-right of route detail) opens a modal that computes estimated driving miles using **Haversine between consecutive geocoded stops within each day/phase × 1.3 road-factor**. Inputs: **Vehicle MPG** (default 20) and **Gas Price $/gal** (default $3.50), both persisted in `localStorage` (`sales_fuel_mpg`, `sales_fuel_price`). Stat cards: TRIP TOTAL · DAILY AVG · WEEKLY (×5 workdays) · MONTHLY (×22 workdays), each showing miles + gallons + cost. Per-Day/Phase breakdown table with stops, miles, gallons, est. cost — sorted by the same numeric/geographic logic as the chip bar (1, 2, 3 → West → Middle → East). Disclaimer footer notes the road factor + that home-base commute is excluded. Verified end-to-end: 19-stop TN Sheriffs route grouped into 5 weekday buckets totals 1948.9 mi @ 20 MPG = 77.96 gal = $272.85; switching MPG to 25 keeps miles stable and updates gallons/cost reactively.
- 2026-05-04: **Sales — Phase chip ordering** — Phase/Day chips are now sorted with: (1) numeric phases first, (2) geographic keyword order (West → Middle/Central → East, North → South), (3) alphabetical fallback. Per user request so a TN-wide route lays out chips left-to-right as `PHASE West Tennessee Division → PHASE Middle Tennessee Division → PHASE East Tennessee Division` instead of alphabetical (East / Middle / West).
- 2026-05-04: **Sales — Visit logging + Trip Recap** — Marking a stop complete now opens a **Log Visit** modal that auto-captures GPS via `navigator.geolocation` (high accuracy, 10s timeout), shows live status (IDLE → CAPTURING → CAPTURED with lat/lng + ±accuracy in meters; gracefully degrades to "save without GPS" on permission denial), and accepts an optional 500-char note. Backend `POST /api/sales/routes/{id}/stops/{idx}/toggle` now accepts a JSON body `{lat, lng, accuracy_m, note}` and persists `visit_lat`, `visit_lng`, `visit_accuracy_m`, `visit_note`, `visited_by` alongside `completed_at`; un-marking clears all visit metadata. New endpoint `GET /api/sales/routes/{id}/recap` returns visited stops sorted by completion time with planned vs actual coordinates, **Haversine distance off-plan** in miles, total visited / completion %, first→last visit duration in minutes, and average off-plan miles. New **TRIP RECAP** button (cyan, next to Print Call Sheet) opens a modal with 5 stat cards + per-visit table (Stop · Phase · Location · Visited At · By · Off-Plan badge color-coded green<0.1mi / cyan<1mi / amber otherwise · Note). Verified end-to-end with mocked Nashville GPS: visit saved, recap modal renders 1/19 (5.3%), note "Met with sheriff" preserved, 196.5 mi off-plan badge correctly displayed (Nashville GPS vs Memphis-planned stop).
- 2026-05-04: **Sales — Print Call Sheet** — Added a "PRINT CALL SHEET" button (amber, top-right of the route detail panel) that opens a printable popup of the **currently filtered** stops. The popup is a self-contained HTML doc styled for paper: route name + salesman + phase + printed-date header, an embedded **Google Static Map** image (markers numbered, polyline connecting stops, capped at 50 markers to stay inside the 8192-char URL limit), and a clean black-on-white stops table with Visited checkbox column + Notes column for field reps. Auto-fires `window.print()` 600ms after load. Respects schema: per-stop layout shows Stop # / Phase / County / Office Address; legacy layout shows Day / Region / Counties+Cities. Uses existing `REACT_APP_GOOGLE_MAPS_KEY`. Verified: button renders, popup opens with correct title, map image URL builds correctly, 19-stop Tennessee Sheriffs route prints all rows.
- 2026-05-04: **Sales module — per-stop schema (Phase / Stop # / County / Office Address / City / State / ZIP)** — Backend `/api/sales/routes/upload` already handled both schemas; this update completes the frontend. `Sales.jsx` now auto-detects per-stop vs route-plan schema by sniffing the stops array (presence of `office_address`/`state`/`zip`/`stop_num`) and switches the table headers accordingly: per-stop shows **Phase · Stop # · County · Office Address · City · State · ZIP**, legacy still shows **Day · Region · Counties · Start · End**. Added a **Phase/Day filter chip bar** above the map (`ALL (n) · PHASE 1 (n) · PHASE 2 (n) · …`) — naturally sorted, click filters both the stops table AND map markers/polyline. Map markers now use the actual `Stop #` as the label (instead of row index) for per-stop schema; popups show "Phase X · Stop Y" header + full office address + "City, State ZIP". Added `useJsApiLoader` so the Google Maps JS loads on the standalone Sales route (was relying on Stark Dashboard pre-loading it). Upload modal hint now lists both supported schemas. Verified end-to-end: 4-stop test xlsx uploaded with Phase 1/Phase 1/Phase 2/Phase 2, all 4 geocoded, filter chips reduce table+map to the correct phase.
- 2026-02-05: **Sales module (route uploads + map)** — New "SALES" Hub card visible to anyone with `permissions.sales` enabled in User Access. New `/sales` page lets users upload `.xlsx` or `.csv` route plans (columns: Week, Day, Region, Counties, Starting City, Ending City) with route name, salesman, and start date. Backend: 5 new endpoints (`/api/sales/routes/upload`, `/list`, `/:id`, `/:id/stops/:idx/toggle`, DELETE) gated by the `sales` module permission. On upload the server geocodes each stop's starting + ending city via the existing Google Geocoding helper (TN bias for ambiguous names), capped at 60 stops to keep upload responsive. Frontend renders a list of uploaded routes (left panel) and a detail panel (right) with: route metadata, a Google Map showing numbered day markers + a polyline connecting stops in order (cyan = pending, green = completed), and a stops table with check-marks to toggle completion. Verified live: 19-stop TN Sheriffs sample uploaded and 19/19 geocoded successfully. New collection: `sales_routes`.
- 2026-02-05: **User Activity admin module (futureroads only)** — New `/admin/user-activity` page accessible only to user `futureroads` from a new "USER ACTIVITY" Hub card. Two panels:
  • **Currently Online** (auto-refreshing every 5s): username, IP, current page, voice-session active badge, last heartbeat, session start, total duration. Driven by a 20-second client-side heartbeat from a new `useActivityHeartbeat` hook mounted in `App.js` via `<ActivityHeartbeatBridge>`.
  • **Session History** with filters (1/7/30/90 days, username search, free-text search) showing login/logout times, session duration, every page visited (with per-page time spent), and AEDA voice usage (sessions count + total seconds). CSV export via `GET /api/admin/activity/export.csv?days=&user=&token=` (token-as-query for browser download). 
  Backend: 4 new endpoints (`/heartbeat`, `/end`, `/online`, `/history`, `/export.csv`) + a 60-second `_activity_session_finalizer_loop` background task that finalizes stale sessions (>5min no heartbeat) and prunes records older than 90 days. Stark Dashboard's mic handler exposes `window.__aedaVoiceActive` so the heartbeat reports voice usage. New collection: `user_activity_sessions`. Verified live: heartbeat creates session, online list shows the user with current route, history+CSV return data, non-admin (stark) gets 403 Admin only.
- 2026-02-05: **AEDA refinements** — (1) **Name pronunciation**: stopped saying "pronounced AID-uh" when introducing herself. Now just says "My name is AEDA" — pronunciation is implicit in how she speaks the word. (2) **Subscriber knowledge**: strengthened the LIVE OPERATIONAL CONTEXT framing in the briefing-as-conversation-item with explicit guidance: *"When I ask 'how is [SUBSCRIBER] doing?' you MUST scan the Per-subscriber readiness section."* Plus aliases and a fallback phrasing (*"I don't see [name] in the active subscriber list"*) so she stops dodging subscriber-specific questions. (3) **Voice command "close map"**: registered new `close_map` tool on the realtime session with handler `handleAedaCloseMap`. Whisper-fallback regex `(close|exit|hide|dismiss|leave) ... map` triggers it directly so map closes even if AEDA only speaks about it.
- 2026-02-05: **Dashboard load speed + display polish + AEDA race fix** — (1) **Dashboard-data now response-cached** (`_dashboard_data_cache`, 2-min TTL, lock-guarded). Was taking 40+ seconds on repeated calls (Readisys + 32 per-subscriber API fan-outs rebuilt every time). Now: cold 2.7s, warm 0.1s. This is the #1 source of the "takes a long time for percentage to get valid data" complaint. (2) **Gauge placeholders changed "—" → "0.0%"** on both Stark and main Dashboard. (3) **DI feed percent-ready trend message hidden until readiness loads** — was still falling back to `totals.percent_ready` (wrong 90.8%) if the dashboard-data endpoint hadn't responded yet. Now we only emit the trend message once `readiness?.pct_ready` is available, so the "GOOD JOB! Percent ready improved from 85.8% to 90.8%" false positive is gone. (4) **"Active response in progress" error** — added `responseActiveRef` to track OpenAI response lifecycle separately from `isSpeakingRef` (responses are active between `response.created` and `response.done`, including while tool calls are pending, not just during audio playback). Dispatcher now skips redundant `response.create` calls when a response is already in flight. Also suppressed the error toast since it's harmless.
- 2026-02-05: **AEDA latency + interruption fixes** — (1) **Startup time dropped from 10–15s → ~0.6s**: `/api/realtime/session` was synchronously refreshing the entire support-dashboard cache (`_support_dashboard_data_core`) on every mic click, which blocks on Readisys + 32 per-subscriber API fan-outs. Now we trust the 5-min background warmer and only kick a non-blocking `asyncio.create_task` if the cache is cold. AEDA starts speaking ~0.6s after the mic click. (2) **Stop interruptions**: server VAD `interrupt_response` flipped to `false`, threshold raised 0.5→0.75, silence_duration 500→700ms — these together prevent background noise / coughs / room HVAC from cutting AEDA off mid-sentence. Most importantly, the frontend now **disables the mic track entirely while AEDA is speaking** (toggling `track.enabled = false` on `response.audio.delta`, back to `true` on `response.done`) — guarantees zero-input during her replies. Echo cancellation already on, so no feedback loop either.
- 2026-02-05: **DI feed numbers + AEDA "active response" race fix** — (1) The AI Overwatch ticker was reading `totals.percent_ready` and `totals.prev_percent_ready` (raw Readisys, ~90.8%) instead of the dashboard-aligned `dsc.get('ready') / total_monitored` (~84.6%). Showed wrong "GOOD JOB! Percent ready improved from 85.8% to 90.8% (+5%)" messages. Backend `/api/support/dashboard-data` readiness payload now uses the same calculation as the gauge; frontend Stark/Dashboard `todayPct/prevPct` switched from `totals.percent_ready` to `readiness?.pct_ready`. (2) Eliminated `response.cancel + response.create` race in the Whisper-fallback path that produced "Conversation already has an active response in progress" errors. Tool-dispatcher path now only fires `response.create` when `isSpeakingRef.current === false`. Whisper fallback no longer issues its own response — it just drives the map; AEDA's natural reply still plays.
- 2026-02-05: **Two refinements** — (1) **Readiness placeholder during initial load**: Stark Dashboard previously fell back to `totals.percent_ready` (~90.8% from raw Readisys) before `/api/support/dashboard-data` arrived with the correct adjusted % (~86%). Now the gauge labels show "—" and the needles hold at 0 until accurate data loads, eliminating the ~3-second flash of the wrong number. (2) **Robust voice "near" intent**: dropped the requirement that the operator say the word "AED" in follow-up queries (e.g., "any near Waycross?" after already discussing AEDs). The Whisper-fallback regex now matches `(near|around|by|in|close to|in the area of|surrounding) <location>` regardless of context. Also fixed a map-pan timing bug — `handleAedaFindNearLocation` now queues the pan/zoom + popup-select via `pendingMapPanRef` and replays it inside the `aedPins` effect, so the map zooms correctly even when switching from a filtered subscriber view to "all".
- 2026-02-05: **AEDA location-based AED lookup ("Are there AEDs near Waycross, GA?")** — New voice tool `find_aeds_near_location(location, radius_miles=10)` registered on the realtime session. New JWT-authed backend endpoint `GET /api/aeda/aeds-near?location=...&radius_miles=...` wraps the existing voice/aeds-near logic without exposing the voice API key. When invoked, the frontend handler `handleAedaFindNearLocation`: (1) calls the endpoint, (2) switches the map to AEDs mode + ALL subscribers, (3) enters fullscreen, (4) pans the Google Map to the geocoded coordinates and zooms to 11, (5) auto-selects the closest AED via a `pendingSelectAedRef` + useEffect that watches `aedPins` and opens that AED's full popup once the pins reload. The tool's `voice_answer` is returned so AEDA reads back e.g. *"I found 2 AEDs within 25 miles of Waycross, Georgia. The closest is at Georgia Power, 2470 Wadley Road, about 3 miles away, currently ready."* The Whisper-transcript fallback was also extended with a `near|around|by|in|close to <location>` regex so the map opens even if the realtime model talks about the tool instead of calling it. Backend: dashboard-aligned readiness math fix (use `dsc.get('ready')` not `totals.ready`), bug-fixed map fallback (proper `s.subscriber` extraction).
- 2026-02-05: **Three AEDA refinements** — (1) **System status now leads with ADJUSTED %** to match the dashboard hero number; reordered the answer template + trend phrasing so AEDA says "Adjusted Percent Ready is X percent. Actual Percent Ready is Y percent." Also added "system ready" to the trigger list. The `_aeda_sub_cache` is now refreshed on EVERY new session (not relying on the 5-min warmer) so AEDA's numbers always match what the dashboard shows. (2) **Map fallback** — added a Whisper-transcript-side fallback: if the operator's transcribed request matches "show/pull up/find/display/open/locate ... on the map" + a known subscriber name (or alias), the frontend triggers `handleAedaShowAedsOnMap` directly without waiting for AEDA's `response.function_call_arguments.done` event. Means the map opens even if the realtime model talks *about* the tool instead of calling it. (3) **Personalized greeting** — AEDA now greets the operator by first name (e.g., "Good afternoon, Mark, my name is Aid-uh, how can I help you?") unless their username is "stark" (kept generic for the demo account). Driven by `freshUser?.name`/`freshUser?.username`.
- 2026-02-05: **Subscriber aliases + stronger map-tool prompt** — Added `SUBSCRIBER_ALIASES` table (e.g., "Franklin County Sheriff" → "County of Franklin", "Georgia Power Company" → "Georgia Power", "GPC" spoken as "G P C") so AEDA understands customer-facing/colloquial names that differ from the canonical subscriber. Aliases are exposed in the briefing AND returned as `aeda_aliases` in the session payload, which the frontend stores in `aliasMapRef` for a final-fallback resolve in `handleAedaShowAedsOnMap`. Strengthened the MAP COMMAND prompt to "CRITICAL — YOU MUST CALL THE TOOL" with explicit "do not just describe what you would do — actually invoke the tool" guidance to ensure the model triggers `show_aeds_on_map` instead of merely speaking about it.
- 2026-02-05: **AEDA briefing pushed via conversation history** — Realtime model was treating long system-instruction blobs as reference text and refusing to quote inline data. Fix: shrunk session-level instructions to behavior-only (1349 chars), now return the live briefing as a separate `aeda_briefing` field; frontend pushes it as `conversation.item.create` (role=user, type=input_text) right after the data channel opens. The model treats conversation history as authoritative committed context and now answers "How is Georgia Power doing?" with real numbers instead of a polite deflection.
- 2026-02-05: **AEDA per-subscriber status + voice-driven map control** — Extended the AEDA fleet briefing with **per-subscriber readiness** pulled from a new `_aeda_sub_cache` (populated whenever `/api/support/dashboard-data` runs, 10-min TTL). AEDA can now answer "How is Georgia Power doing?" with real numbers (e.g., "Georgia Power is at eighty-seven point two percent ready. They have two hundred eighty-one AEDs total, sixteen with active issues: one expiring batteries/pads, ten reposition, five not present, twenty lost contact"). Also exposed a `KNOWN SUBSCRIBERS` list in instructions for exact spelling during tool matching. **New voice command**: registered `show_aeds_on_map(subscriber)` tool on the Realtime session. When the operator says "Show me where Georgia Power's AEDs are" (or "find [sub] on the map", "pull up [sub]", etc.), AEDA calls the tool via the WebRTC data channel; frontend handler `handleAedaShowAedsOnMap` fuzzy-matches the name against `aedSubscribersList`, switches `mapMode="aeds"`, sets `aedSubscriber=matched`, and enters fullscreen. AEDA acknowledges verbally and the tool output is looped back so she can continue the conversation.
- 2026-02-05: **AEDA "system status" template** — Added explicit "SYSTEM STATUS ANSWER TEMPLATE" to the fleet briefing that AEDA quotes verbatim when asked "system status", "status report", "readiness", or "how are we doing". Reply pattern: "Percent Ready is X.X percent. Adjusted Percent Ready is X.X percent. System trend versus yesterday: actual readiness is up/down/flat X.X percent, and adjusted readiness is up/down/flat X.X percent." Trend logic handles flat (±0.1%), up, and down deltas per component independently.
- 2026-02-05: **AEDA now answers live fleet questions** — Extended the `/api/realtime/session` endpoint with `_build_aeda_fleet_briefing()` helper that compiles a concise voice-friendly briefing at session-start and injects it into AEDA's system instructions. Briefing includes: total monitored AEDs, ready counts, actual + adjusted readiness %, breakdown of active issues by type (expired B/P / expiring / not ready / reposition / not present / lost contact), unresolved/opened notification counts, 24h resolutions, and a top-subscribers list aggregated from `notified_aeds` (e.g. "GPC: 192 unresolved (183 expired b/p, 8 expiring batt/pads, 1 lost contact)"). AEDA can now answer "What's my readiness today?" or "How's GPC?" with real numbers instead of a placeholder. Uses the existing 5-min Readisys status cache + in-memory MongoDB aggregation (no extra per-subscriber API fan-out, so session creation stays fast).
- 2026-02-05: **AEDA Voice migrated to OpenAI Realtime API (WebRTC)** — Replaced the `tts-1` (Emergent Universal Key) mic flow on the Stark Dashboard with a live, two-way OpenAI Realtime session. Backend: two new endpoints — `POST /api/realtime/session` (creates ephemeral OpenAI Realtime session with voice `ash` and AEDA/JARVIS instructions) and `POST /api/realtime/negotiate` (forwards browser SDP offer to OpenAI, returns SDP answer). Uses direct `OPENAI_API_KEY` from `backend/.env` (bypasses exhausted Emergent Universal Key budget). Frontend: rewrote Stark Dashboard mic button — first click opens mic + WebRTC peer connection, AEDA greets first via data-channel `response.create` event, user can then converse naturally; second click ends session. Audio routed through a hidden `<audio autoplay>` element; mic track stopped + peer connection closed on disconnect or unmount. `stark-voice-mic-btn` data-testid added. Model: `gpt-4o-realtime-preview-2024-12-17`.
- 2026-05-03: **Runtime apiBase fix** — Replaced build-time `NODE_ENV` check in `apiBase.js` with a runtime `window.location.hostname` check. Reason: a stale `REACT_APP_BACKEND_URL` (pointing to `pulse-ops.emergent.host`) was being baked into production builds, causing every API request from `cardiac-solutions.ai` to hit a different origin and fail with 520 + CORS. Fix: any non-localhost hostname now uses `''` (relative URLs / same-origin), regardless of build-time env vars. localhost dev workflow still respects `REACT_APP_BACKEND_URL` for tunneling.
- 2026-05-03: **DI Overview mode now truly rolls up** — Previously, setting any DI category (Email Events / AED Resolutions / Engagement Summary) to "overview" still emitted one line per subscriber, indistinguishable from "details" at scale. Now overview produces a SINGLE consolidated message per category covering all subscribers (e.g. "AED RESOLUTIONS 24h — across 4 subscribers: 12 fully resolved, 3 partially resolved." or "EMAILS 24h — across 5 subscribers: 47 delivered, 28 opened, 4 clicked, 1 bounced."). Drastically reduces ticker noise on Stark Dashboard for admins/users who want a high-level view without disabling the category entirely.
- 2026-05-03: **Support Dashboard hub card + access toggle** — Added a SUPPORT DASHBOARD card (LIVE) to the Command Center Hub at Module #4 with a lifebuoy icon. New dedicated `/support-dashboard` route works for any authenticated user (not just `dashboard_type=support`). Added a "Support Dashboard" toggle to the User Access management form's MODULE ACCESS list so admins can grant/revoke per-user visibility of the card.
- 2026-05-01: **Stark Dashboard expanded readiness trend modal** — Made the System Status sparklines clickable. Opens a full-width readiness trend modal (`ReadinessTrendModal`) showing both Adjusted (solid green) and Actual (dashed gray) % lines overlaid on a 1100×360 SVG chart with Y-axis ticks, X-axis date labels, legend, and 7D/14D/30D/60D window selector. Hovering any day reveals a 5-card breakdown: SELECTED DAY · ADJUSTED · ACTUAL · AEDs NOTIFIED · AEDs RESOLVED. Backend `/api/support/readiness-history` now augments each daily point with `notified_count` and `resolved_count` aggregated from `notified_aeds`. Click propagation stopped on the sparkline so it doesn't also open the existing System Readiness Breakdown modal.
- 2026-05-01: **Stark Dashboard sparklines** — Mirrored the Support Dashboard's 7-day sparklines onto the Stark Dashboard System Status panel. Two compact sparklines (130×28) appear under the gauge labeled ADJ 7-DAY and ACT 7-DAY, sourced from the same `/api/support/readiness-history?days=7` endpoint. Same color logic (green=up, red=down, blue=flat), tooltips, and fallback states (1-point dot, 0-point hidden).
- 2026-05-01: **Settings dropdown + Detail Messages** — Replaced the standalone "CONTACTS" button on the Support Dashboard with a "SETTINGS" dropdown containing two items: CONTACTS (existing) and DETAIL MESSAGES (new). The new feature lets Mark create reusable templates of "Details" text keyed on (status, subscriber, AED model). Backend: 4 new endpoints — `GET/POST /api/support/detail-messages`, `DELETE /api/support/detail-messages/{id}`, `GET /api/support/detail-messages/lookup`, plus helper `GET /api/support/aed-models` returning distinct AED models seeded with 14 common models. NotificationModal now fetches all templates on mount and pre-populates the Details cell for each AED row via best-match (subscriber+model > subscriber > model > ALL/ALL). Mark can still edit per-row before sending.
- 2026-04-30: **Reversed trend colors + 7-day sparklines** — Corrected the readiness trend semantics: UP=GREEN (improvement), DOWN=RED (regression), FLAT=BLUE line. Added a 72×18 SVG sparkline below each percentage showing the last 7 daily snapshots with color driven by net 7-day direction and a tooltip like "7-day: 84.5% → 82.0% (Δ -2.5)". Backend additions: new `readiness_daily` collection (auto-upserted on each `/api/support/dashboard-data` call) and new endpoint `GET /api/support/readiness-history?days=7` returning ordered daily points.
- 2026-04-30: **Readiness Trend Arrows** — Added trend indicators to the right of both ACTUAL READY and ADJUSTED READY percentages in the Notified AEDs Readiness Tracker bar. Per user spec: UP=red (rising), DOWN=green (dropping), flat=blue horizontal line (<0.05% change). Hover tooltip shows `"Yesterday: X% · Δ ±Y"`. Backend now exposes `prev_pct_ready` alongside existing `prev_pct_ready_adjusted` in `/api/support/dashboard-data` → `readiness` block. Verified live via DOM inspection: both arrows rendering correctly with title tooltips.
- 2026-04-30: **DELIV/OPEN consistency fix** — Two-pronged solution to the "OPEN > DELIV" anomaly (e.g. Opelika PD: 4 sent / 3 deliv / 2 open): (a) Added runtime `delivered_effective = max(delivered, opened_by_to, clicked)` safety guard in `/api/support/subscriber-engagement` — DELIV can never be less than OPEN or CLICKED in reports. (b) Added `_backfill_delivered_from_opens_core` helper + `POST /api/support/backfill-delivered-from-opens` endpoint. For records with `open_count>0` or `click_count>0` but no `delivered_at`, stamps the earliest open/click/sent timestamp as `delivered_at` and sets `delivered_inferred: true`. Wired into the 30-min auto-loop. Reason: SendGrid webhook ordering or legacy pre-webhook sends sometimes left records with opens but no delivery event.
- 2026-04-29: **Auto-alert on send failures** — Added `_send_send_failure_alert` helper that fires a styled HTML alert email to `SEND_FAILURE_ALERT_TO` (default `iq.ai.solutions@gmail.com`) whenever a notification send fails — both the SendGrid non-2xx path and the exception path are wired in. Alert includes timestamp, user (sent_by), subscriber, recipient addresses, subject, result summary, error_type, error_message, and a link to `/admin/email-errors`. Verified end-to-end: forced a `KeyError` failure → log entry recorded → alert email dispatched (confirmed via backend log). Added `SEND_FAILURE_ALERT_TO` env var.
- 2026-04-29: **Admin Email Activity Monitor** — New admin-only page at `/admin/email-errors` (also surfaced as a Hub module card and a small `EMAIL LOG` button on the Support Dashboard top bar visible only when `user.role==="admin"`). Backend exposes `GET /api/admin/email-activity?days=&user=&status=` (admin-gated) returning the last 500 sends with totals + per-user breakdown + distinct-users list. UI features: time-window selector, user-filter dropdown, ALL/SUCCESS/FAILED chips, free-text search, click-row drawer with full details (WHEN/USER/SUBSCRIBER/TO/CC/BCC/SUBJECT/SENDGRID RESPONSE/SG-Message-ID/LINKED AEDs/DELIVERY EVENTS, plus error_type + error_message for failed sends). Also fixed the send-notification flow to **persist failures** (exceptions, network errors, etc.) into `notification_history` with `success=False`, `error_type`, `error_message` — previously only non-2xx responses were logged. Verified live as `futureroads`.
- 2026-04-29: **Auto-backfill linked_sentinel_ids** — Added `_backfill_linked_sids_core` helper (and `POST /api/support/backfill-linked-sids` manual trigger). For each legacy `notification_history` record with empty/missing `linked_sentinel_ids`, finds notified_aeds for the same subscriber whose `notification_dates` (or first/last_notified_at) match the email's `sent_at` ±10 minutes, and stamps the matched sentinel IDs back onto the email record. Now runs silently after each 30-min status refresh in `_notified_aeds_daily_loop`. Verified on preview: linked 262 sentinel IDs to 2 legacy records.
- 2026-04-29: **Per-AED targeted NOTIFY** — Each row in the SubscriberDetailModal's ACTIVE ISSUES tab now has a dedicated NOTIFY button that opens the compose modal pre-filtered to just that single AED (so Mark can chase a specific stuck device without spamming the full subscriber). Compose modal title shows a clear "SINGLE AED: {sentinel_id}" amber badge. Implemented via new `targetSentinelId` prop on `NotificationModal`.
- 2026-04-29: **Subscriber Status Icons + Detail Modal** — Replaced the static "SENT" badge in the ACTIONS column with a 4-state notification status indicator: 🔴 NEEDS NOTIFY (active issues, zero sent), 🟡 PARTIAL (some sent, some new still pending), 🟡 STALE >14d (notified but unresolved >14d), 🟢 ALL NOTIFIED (every active issue covered). Clicking a subscriber name opens a new full-screen `SubscriberDetailModal` with three tabs: ACTIVE ISSUES (per-AED Sentinel ID/location/dates/days-since-notified/email-opened), RESOLVED, and EMAIL HISTORY (per-email DELIV/OPEN/CLICK/BOUNCE tracking). Backend: added `GET /api/support/subscriber/{subscriber}/breakdown` aggregator endpoint and `stale_unresolved` field on each subscriber in `/api/support/dashboard-data`.
- 2026-04-28: **Bigger NotificationModal** — Expanded the Send Notification modal from 1100px to 1500px wide and 92vh→95vh tall. The email preview area (white card) now gets significantly more horizontal/vertical room, making it much easier to scan multiple AED rows, full location strings, and camera images at a glance before sending.
- 2026-04-28: **Backfill matching relaxed + diagnostics** — `_backfill_email_opens_core` now (1) treats legacy `notification_history` records with `open_count > 0` but empty `events` array as TO-opens (preserves count without per-event log), (2) adds a "subscriber fallback" path that credits the most recent open from a subscriber to that subscriber's unresolved AEDs when neither linked_sentinel_ids nor ±10-min time-window match, (3) returns rich diagnostic counters (`candidate_aeds`, `to_recipient_opens`, `matched_via_linked_sids`, `matched_via_time_window`, `matched_via_subscriber_fallback`, `unmatched_*`). Frontend toast shows the matched count plus the most actionable reason when 0 are credited. Resolves the production case where 58+ opens existed but none lifted Adjusted % above Actual.
- 2026-04-28: **SG DEBUG modal** — Added a purple "SG DEBUG" button in the Support Dashboard top bar that opens `SendGridDebugModal`. Shows: total webhook events logged, last 30 webhook events with EVENT type / EMAIL / SG MESSAGE ID / MATCHED? Y/N, and the last 20 sent emails (notification_history) with their delivery + open + click counts. Surfaces sg_message_id mismatches, missing webhook config, or the simple "no opens yet" case. Calls existing `GET /api/support/sendgrid-debug` endpoint.
- 2026-04-28: **Auto-Backfill Email Opens** — Refactored backfill into reusable `_backfill_email_opens_core()` helper; now runs automatically after every 30-min `notified_aeds` status refresh in `_notified_aeds_daily_loop`. Logs `[NOTIFIED_AEDS] Auto-backfill credited N AEDs as opened` whenever new matches are found. Adjusted % Ready now self-corrects continuously without requiring manual SYNC OPENS clicks.
- 2026-04-28: **SYNC OPENS button** — Wired up a new emerald-bordered "SYNC OPENS" button in the Notified AEDs Readiness Tracker bar that calls existing `POST /api/support/backfill-email-opens`. Scans `notification_history` for TO-recipient opens and back-stamps `email_opened=true` on matching `notified_aeds` (via `linked_sentinel_ids` direct match OR ±10min time-window fallback for legacy records). Resolves the "Adjusted % Ready = Actual % Ready" issue caused by historical email opens never being linked to the AEDs they referenced. Toast reports `newly_matched_aeds` and current opened/unresolved totals.
- 2026-04-28: **Engagement Modal Math Hardening** — `SubscriberEngagementModal` now recomputes DELIV %, OPEN %, BOUNCE %, CLICK % locally on the frontend using `emails_sent` as the denominator, regardless of what the backend returns. Guarantees the displayed math always matches the disclaimer even if a deployed environment serves a stale JS bundle. New explicit "FORMULAS:" footer spells out each formula. (Resolves user-reported math mismatch on deployed build.)
- 2026-04-26: **SendGrid Open/Click Tracking** — Built `POST /api/sendgrid/events` webhook to receive SendGrid event batches (delivered/open/click/bounce/dropped/spamreport). Captures `X-Message-Id` on every send and correlates incoming events to `notification_history` records, updating `delivered_at`, `open_count`, `click_count`, `first_opened_at`, `bounced` flags. New TRACKING column on the HISTORY modal shows DELIV/OPEN/CLICK/BOUNCED badges per email. Replaced the old prompt-based "TEST EMAIL" button with a full `TrackingTestModal` that polls live and shows the lifecycle (SENT → DELIVERED → OPENED → CLICKED) with an event log. Added `/api/support/sendgrid-debug` diagnostic endpoint for troubleshooting webhook delivery.
- 2026-04-26: **Stark Map Fullscreen + Satellite View** — Added FULLSCREEN/EXIT FULLSCREEN button on Stark Dashboard map (covers entire viewport, ESC to close, auto-resizes Google Map). Added MAP/SATELLITE style toggle (uses Google's hybrid imagery). Color-coded AED dots by live Readisys status: green=READY, red=NOT READY/LOST CONTACT/EXPIRED B/P, yellow=other. Popup shows current status with matching color badge.
- 2026-04-26: **Voice API for AED Locations** — Added `GET /api/voice/aeds-near` endpoint accepting either `location` (free-text geocoded server-side via Google Maps) or explicit `lat`+`lng`. Returns nearest AEDs within `radius_miles` (default 25) along with each device's live status from Readisys, distance in miles, and a natural-language `voice_answer` ready for AEDA/voice apps. Auth via `X-Voice-API-Key` header. Production URL: `https://cardiac-solutions.ai/api/voice/aeds-near`.
- 2026-04-25: **Stark Dashboard Map AED Mode + Geocoded Subscriber Data** — Added Subscribers/AEDs toggle, subscriber filter dropdown, and prominent control bar on Stark Dashboard map. Seeded 280 Georgia Power AED locations (from `Geocoded_AED_Full_List.xlsx`), re-geocoded server-side via Google Maps Geocoding API for 63 ROOFTOP + 10 RANGE_INTERPOLATED + 14 GEOMETRIC_CENTER + 3 APPROXIMATE precision tiers. Added auto-seed on backend startup from bundled `subscriber_map_seed.json` so production stays in sync.
- 2026-04-18: Added Image History feature to DeviceListModal — red HISTORY button below each camera image opens modal showing all captured images for that device (stored in MongoDB, auto-populated from Readisys API)
- 2026-04-17: Created "Stark" dashboard type — MAP as hero center card, DI panel compact below with overview mode, assigned to user Stark. Skip-to-dashboard login when only "dashboard" module enabled.
- 2026-04-16: Backfill logic for notified AEDs from historical notification_history records; percent format fixed to XX.X%; uses /devices API for accurate device list
- 2026-04-16: Added Notified AEDs Detail View modal — drill into each tracked device with full notification timeline, days since notified, current vs original status, filter/sort by subscriber/status/age
- 2026-04-16: Built Notified AEDs Readiness Tracker — tracks each AED in sent notification emails, shows Actual vs Adjusted readiness %, daily auto-refresh of statuses, expandable card with issue type & subscriber breakdowns
- 2026-04-15: Switched Map Module to use new `GET /api/map/readiness/subscribers/summary` API — single call replaces N+1 subscriber calls, pins color-coded by readiness %, instant popups
- 2026-04-15: Added editable DETAILS field per AED device in notification email modal (preview + Mailgun HTML)
- 2026-04-14: Added Map Module (Google Maps), stat card filters, progress bars, notification history sorting, clickable issue counts, status correction feedback, auto CC
- 2026-04-09: Added Device Detail Drawer (full image, all device info, diagnostics, editable notes)
- 2026-04-09: Added Notification History modal with subscriber filter
- 2026-04-09: Added Batt/Pads Exp, Battery %, Signal, Capture Timestamp columns to email preview
- 2026-04-09: Updated email subject to "AED Report and Action Items"
- 2026-04-09: Renamed section to "AED Batteries and Pads Expired/Expiring"
- 2026-04-08: Bulk imported 48 subscriber contacts from Excel spreadsheet
- 2026-04-08: Camera images integrated from Readisys API
- 2026-04-08: Support Dashboard + dashboard assignment system built
